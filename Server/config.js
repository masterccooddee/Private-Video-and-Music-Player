
let VideoExpireTime = 60 * 60 * 24 * 2; // 預設為 2 天
let VideoAddExpireTime = 60 * 60 * 2; // 預設為 2 小時
let MusicExpireTime = 60 * 60 * 24 * 2; // 預設為 2 天
let MusicAddExpireTime = 60 * 60 * 2; // 預設為 2 小時
let MAX_CONCURRENT_CONVERSIONS = 3; // 最大同時轉換數量
let PreConvertEpisodesCount = 2; // 預轉換的集數數量

export{
    VideoExpireTime,
    VideoAddExpireTime,
    PreConvertEpisodesCount,
    MusicExpireTime,
    MusicAddExpireTime,
    MAX_CONCURRENT_CONVERSIONS
}
